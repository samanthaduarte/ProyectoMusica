using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FormGenerator : MonoBehaviour
{
   
   /* public List<List<string>> GenerateForm(List<string> estructura)
    {
        // AABB, ABAB, ABBA, AAAB, ABCD, ABCA, ABCB, ABCC
        List<List<string>> forms = new List<List<string>>();
        forms.Add(["A", "A", "B", "B"]);
        forms.Add(["A", "B", "A", "B"]);
        forms.Add(["A", "B", "B", "A"]);
        forms.Add(["A", "A", "A", "B"]);
        forms.Add(["A", "B", "B", "C"]);
        forms.Add(["A", "B", "C", "D"]);
        forms.Add(["A", "B", "C", "A"]);
        forms.Add(["A", "B", "C", "B"]);
        forms.Add(["A", "B", "C", "C"]);

        List<string> form = forms[Random.Range(0,forms.Count())];
        return(form);
    }*/

}
